//se va a ejecutar cuando se inician las pruebas.
//lo llamamos desde el archivo jet.config.js
//en éste caso vamos a poner código que le permita ejecutar a node el fecth para obtener el gif en la petición en caso de que no pueda realizar la petición por sí mismo
//instalamos el paquete por consola "npm install whatwg-fetch --save-dev    //-dev indica que instalamos el paquete solo para desarrollo y no para producción

import 'whatwg-fetch';
